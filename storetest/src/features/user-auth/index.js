import User from "./registration-model"
import Login from "./login-model"

export default function (UserAuth) {
  let (UserAuth === false)
  if (User === Login) {
    let (UserAuth === true)
    return ProductPage
  }
}
